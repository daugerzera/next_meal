

export default function NotFound({ error }) {

    return <main className="not-found">
        <h1>Not found</h1>
        <p>Sorry, the page you are trying to reach does no exists!</p>
    </main>
}